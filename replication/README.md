# Replication Harness

Run the quick baseline vs. SCFD comparison from the repository root:

```powershell
python -m replication.minimal_cartpole
```

Use `--update-expected` to refresh the saved metrics in `expected.json` after rerunning with different seeds or horizons.
